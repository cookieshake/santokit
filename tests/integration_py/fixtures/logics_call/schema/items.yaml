version: 1
tables:
  items:
    connection: main
    id:
      name: id
      generate: ulid
    columns:
      name:
        type: string
        nullable: false
      owner_id:
        type: string
        nullable: true
