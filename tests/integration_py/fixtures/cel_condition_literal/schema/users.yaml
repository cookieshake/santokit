version: 1
tables:
  users:
    connection: main
    id:
      name: id
      generate: client
    columns:
      email:
        type: string
        nullable: false
        unique: true
      name:
        type: string
        nullable: true
