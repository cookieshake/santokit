tables:
  users:
    select:
      - roles: [admin]
        columns: ["*"]
      - roles: [viewer]
        columns: ["id", "normal", "s_sensitive"]
    insert:
      - roles: [admin]
        columns: ["normal", "s_sensitive"]
    update:
      - roles: [admin]
        columns: ["normal", "s_sensitive"]
    delete:
      - roles: [admin]
