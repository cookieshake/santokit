tables:
  users:
    select:
      - roles: [admin]
        columns: ["*"]
      - roles: [authenticated]
        columns: ["id", "normal", "s_sensitive"]
    insert:
      - roles: [authenticated]
        columns: ["normal", "s_sensitive"]
    update:
      - roles: [authenticated]
        columns: ["normal", "s_sensitive"]
    delete:
      - roles: [authenticated]
