version: 1
tables:
  files:
    connection: main
    id:
      name: id
      generate: ulid
    columns:
      name:
        type: string
        nullable: false
      avatar_key:
        type: file
        bucket: main
        onDelete: cascade
        nullable: true
      backup_key:
        type: file
        bucket: main
        onDelete: preserve
        nullable: true
      optional_key:
        type: file
        bucket: main
        nullable: true
