tables:
  users:
    select:
      - roles: [authenticated]
        columns: ["id", "name", "email", "avatar_url", "bio"]
    insert:
      - roles: [authenticated]
        columns: ["name", "email", "avatar_url", "bio"]
    update:
      - roles: [authenticated]
        columns: ["name", "avatar_url"]
    delete:
      - roles: [authenticated]
