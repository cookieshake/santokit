tables:
  users:
    select:
      - roles: [admin]
        columns: ["*"]
      - roles: [basic]
        columns: ["id", "name", "avatar_url", "bio"]
    insert:
      - roles: [admin]
        columns: ["name", "email", "avatar_url", "bio"]
    update:
      - roles: [admin]
        columns: ["name", "avatar_url"]
    delete:
      - roles: [admin]
